{
  "name": "subtitle-overlay-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "tauri:dev": "tauri dev",
    "build:web": "tsc && vite build",
    "build": "tauri build --no-bundle",
    "build:mac-app": "tauri build --bundles app"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "typescript": "^5.9.3",
    "vite": "^8.0.16"
  }
}
